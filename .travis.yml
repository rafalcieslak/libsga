language: cpp
compiler:
  - clang
  - gcc
dist: trusty
sudo: required
git:
  submodules: false
before_install:
  - git submodule update --init
  - sudo apt-get -qq update
  - sudo apt-get install -y xorg-dev libglm-dev libassimp-dev
script:
  - mkdir build && cd build
  - cmake ..
  - make